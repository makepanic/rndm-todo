<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal"
    android:padding="2dp" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:background="@drawable/loader_background_normal"
        android:orientation="horizontal" android:padding="1dp">

        <ImageView
            android:id="@+id/list_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:src="@drawable/sort_active_small"
            android:contentDescription="@string/content_user_avatar"
            android:adjustViewBounds="true"
            android:maxHeight="32dp" android:maxWidth="32dp"
            android:minHeight="32dp" android:minWidth="32dp"/>

        <TextView
            android:id="@+id/list_txt"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:gravity="center_vertical"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:layout_weight="1"
            android:textSize="12dp" />
        <ImageView
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="center_vertical"
                android:id="@+id/remove_contact"
                android:background="@android:drawable/ic_menu_close_clear_cancel"
                android:adjustViewBounds="true"
                android:maxHeight="32dp" android:maxWidth="32dp"
                android:minHeight="32dp" android:minWidth="32dp"/>

    </LinearLayout>

</LinearLayout>